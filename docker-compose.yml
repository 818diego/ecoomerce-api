services:
  ecommerce-api:
    build: .
    container_name: ecommerce-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3011:3011"
    networks:
      - mariadb_default

networks:
  mariadb_default:
    external: true
